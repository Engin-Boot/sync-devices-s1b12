name: Build and Run

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
  runs-on: ubuntu-latest

  cppcheck --enable=all --error-exitcode=1 --suppress=missingIncludeSystem  Patient/Patient.cpp 